#include <stdio.h>

int main()
{
    unsigned long long n;
    int a = 0;
    int b = 0;
    for (int i = 1; i <= 64;i++)
        if(n)
            a+=1;
        else
            b+=1;
    printf("%d\n", a);
    printf("%d", b);
}